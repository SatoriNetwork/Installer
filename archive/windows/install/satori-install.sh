# this must be an exe or a bat script for windows or powershell or something.
# will setup directory to mount to in appdata %APPDATA%/local/Satori/wallet and /config and /data and /models
# will setup a run script in startup folder to wait 5 minutes then run the satori container
# will execute run script (will open browser for ui)
# on first run, the satori hanlde a setup process for asking for password etc.
# I'll have to make a version of this for mac and linux too.